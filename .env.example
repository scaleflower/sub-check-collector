# GitHub Personal Access Token (可选,但强烈建议配置)
# 获取方式: https://github.com/settings/tokens
# 需要权限: public_repo
GITHUB_TOKEN=your_github_token_here

# 搜索关键字 (逗号分隔)
SEARCH_KEYWORDS=free,v2ray

# 定时执行规则 (cron 表达式)
# 示例:
#   0 2 * * *     - 每天凌晨 2 点
#   0 */6 * * *   - 每 6 小时
#   0 0 * * 0     - 每周日凌晨
SCHEDULE_INTERVAL=0 2 * * *

# 输出文件路径
OUTPUT_FILE=./output/subscriptions.md

# 最大搜索仓库数
MAX_REPOSITORIES=30

# config.yaml 文件路径 (自动更新订阅链接)
CONFIG_YAML_PATH=./config.yaml

# 最低 star 数量 (0 表示不限制)
MIN_STARS=0

# 最大更新天数 (超过此天数未更新的仓库将被忽略)
# 推荐值: 30-90 天
MAX_DAYS_SINCE_UPDATE=90

# 是否验证链接有效性 (true/false)
# 开启后会检测每个订阅链接是否可访问,过滤掉无效链接
VALIDATE_LINKS=false

# 链接验证超时时间 (毫秒)
# 默认 10 秒,超过此时间未响应的链接将被视为无效
LINK_VALIDATION_TIMEOUT=10000

# 链接验证并发数
# 默认 10,同时验证的链接数量,建议根据网络情况调整
# 值越大验证越快,但可能触发服务器限流
LINK_VALIDATION_CONCURRENCY=10

# 日志目录
LOG_DIR=./logs

# 是否启用文件日志 (true/false)
# 设置为 false 则仅在控制台输出日志
ENABLE_FILE_LOG=true
